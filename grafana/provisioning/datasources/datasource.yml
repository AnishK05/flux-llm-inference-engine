apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    # Published host port, same pattern as scraping the API. Compose DNS
    # (http://prometheus:9090) can fail when inter-container routing is blocked.
    url: http://host.docker.internal:9090
    isDefault: true
    editable: false
